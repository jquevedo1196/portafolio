---
interface Props {
  title: string;
  description?: string;
}

const { title, description = "Jesús Quevedo — Principal DevOps Engineer / SRE" } = Astro.props;
const currentPath = Astro.url.pathname;
const base = import.meta.env.BASE_URL;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&family=JetBrains+Mono:wght@400;500;700&display=swap" rel="stylesheet" />
    <title>{title} | Jesús Quevedo</title>
  </head>
  <body>
    <nav class="nav">
      <div class="container">
        <a href={base} class="nav-logo">jquevedo<span>.dev</span></a>
        <ul class="nav-links">
          <li><a href={base} class:list={[{ active: currentPath === base }]}>Home</a></li>
          <li><a href={`${base}about/`} class:list={[{ active: currentPath.includes("/about") }]}>About</a></li>
          <li><a href={`${base}projects/`} class:list={[{ active: currentPath.includes("/projects") }]}>Projects</a></li>
          <li><a href={`${base}blog/`} class:list={[{ active: currentPath.includes("/blog") }]}>Blog</a></li>
          <li><a href={`${base}resume/`} class:list={[{ active: currentPath.includes("/resume") }]}>Resume</a></li>
        </ul>
      </div>
    </nav>

    <main>
      <slot />
    </main>

    <footer class="footer">
      <div class="container">
        <div class="footer-links">
          <a href="https://linkedin.com/in/jquevedo1196" target="_blank" rel="noopener">LinkedIn</a>
          <a href="https://github.com/jquevedo1196" target="_blank" rel="noopener">GitHub</a>
          <a href="mailto:jenriqueqt@gmail.com">Email</a>
        </div>
        <p class="footer-copy">&copy; 2025 Jesús Quevedo. Built with Astro.</p>
      </div>
    </footer>
  </body>
</html>

<style is:global>
  @import '../styles/global.css';
</style>
